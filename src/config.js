// Base de l'API backend (même valeur que dans App.jsx / main.jsx).
// Extraite ici pour être importable par des modules isolés (ex. l'adapter offres)
// sans créer d'import circulaire vers App.jsx.
export const API_BASE = "https://provision-backend-production.up.railway.app";
